import 'package:flutter/material.dart';

class NewInvoicePge extends StatelessWidget {
  const NewInvoicePge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CREATE NEW INVOICE"),
      ),
    );
  }
}
